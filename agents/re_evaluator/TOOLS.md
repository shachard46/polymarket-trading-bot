# Tools

Source of truth: runtime-enforced tool access is defined in `agent.yaml` and validated by the orchestrator.

- `evaluate_market_metrics`: same as evaluator; used when the market already has a report under Active Research.
