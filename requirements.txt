# Orchestrator runtime dependencies (root-level).
# Skill-specific dependencies live alongside each skill (e.g. skills/market-scraper/requirements.txt).
pydantic>=2.6
PyYAML>=6.0
cmdop>=2026.4.7.2
tenacity>=8.0
